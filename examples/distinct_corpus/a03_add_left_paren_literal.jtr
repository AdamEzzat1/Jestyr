// a03 — laundering shape 2: `(a + 1) + b`.
distinct Id = i64
distinct Acct = i64
fn main() -> i32 {
    let a: Id = 1 as Id
    let b: Acct = 2 as Acct
    print_int(((a + 1) + b) as i32)
    return 0
}
