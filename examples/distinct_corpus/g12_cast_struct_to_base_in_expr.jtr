// g12 — the same expression-position cast over a struct base: `(w as Pt).x`.
struct Pt { x: i32, y: i32 }
distinct W = Pt
fn main() -> i32 {
    let w: W = Pt { x: 3, y: 4 } as W
    print_int((w as Pt).x)
    return 0
}
