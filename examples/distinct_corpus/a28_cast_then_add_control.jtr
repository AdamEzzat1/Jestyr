// a28 — POSITIVE CONTROL for a01: the same mixing, spelled with explicit casts.
distinct Id = i64
distinct Acct = i64
fn main() -> i32 {
    let a: Id = 1 as Id
    let b: Acct = 2 as Acct
    print_int(((a as i64) + (b as i64)) as i32)
    return 0
}
