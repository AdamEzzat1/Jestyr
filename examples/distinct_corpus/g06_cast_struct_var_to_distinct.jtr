// g06 — casting a STRUCT variable into a distinct-over-struct.
struct Pt { x: i32, y: i32 }
distinct W = Pt
fn main() -> i32 {
    let q: Pt = Pt { x: 3, y: 4 }
    let w: W = q as W
    print_int(w.x)
    return 0
}
