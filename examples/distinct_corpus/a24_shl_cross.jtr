// a24 — shift with a foreign distinct as the shift amount.
distinct Id = i64
distinct Acct = i64
fn main() -> i32 {
    let a: Id = 1 as Id
    let b: Acct = 2 as Acct
    print_int((a << b) as i32)
    return 0
}
