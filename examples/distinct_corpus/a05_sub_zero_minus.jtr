// a05 — laundering shape 4: `a - (0 - b)`.
distinct Id = i64
distinct Acct = i64
fn main() -> i32 {
    let a: Id = 3 as Id
    let b: Acct = 2 as Acct
    print_int((a - (0 - b)) as i32)
    return 0
}
