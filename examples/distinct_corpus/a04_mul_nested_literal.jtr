// a04 — laundering shape 3: `a * (b * 2)`.
distinct Id = i64
distinct Acct = i64
fn main() -> i32 {
    let a: Id = 3 as Id
    let b: Acct = 2 as Acct
    print_int((a * (b * 2)) as i32)
    return 0
}
