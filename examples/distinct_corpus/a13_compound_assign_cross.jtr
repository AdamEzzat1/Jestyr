// a13 — compound assignment across two id spaces: `a += b`.
distinct Id = i64
distinct Acct = i64
fn main() -> i32 {
    var a: Id = 1 as Id
    let b: Acct = 2 as Acct
    a += b
    print_int(a as i32)
    return 0
}
