// a02 — laundering shape 1: `a + (b + 1)`.
distinct Id = i64
distinct Acct = i64
fn main() -> i32 {
    let a: Id = 1 as Id
    let b: Acct = 2 as Acct
    print_int((a + (b + 1)) as i32)
    return 0
}
