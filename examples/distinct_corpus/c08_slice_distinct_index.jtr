// c08 — indexing a distinct-over-slice.
distinct Buf = []i64
fn main() -> i32 {
    let arr: [3]i64 = [1, 2, 3]
    let b: Buf = slice(i64, arr.ptr, 3) as Buf
    print_int(b[1] as i32)
    return 0
}
